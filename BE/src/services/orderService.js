import { Order } from "../models/Order.js";

const orderService = {
  addOrder: async (dataOrder) => {},
  getAllOrders: async () => {},
  getOrderById: async (id) => {},
  udpateOrder: async (id, dataOrder) => {},
  deleteOrder: async (id) => {},
};

export default orderService;
