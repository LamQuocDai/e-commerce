import { Cart } from "../models/Cart.js";

const cartService = {
  addCart: async (dataCart) => {},
  getAllCarts: async () => {},
  getCartById: async (id) => {},
  updateCart: async (id, dataCart) => {},
  deleteCart: async (id) => {},
};

export default cartService;
